/** Compatibility identity facade for the canonical sn_create module. */

export {
  createToolModule,
  definition,
  handler,
  moduleInputSchema,
  resolveCreateAccess,
  schema,
} from "./create-module.js";
