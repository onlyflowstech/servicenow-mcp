/** Compatibility identity facade for the canonical sn_update module. */

export {
  definition,
  handler,
  moduleInputSchema,
  resolveUpdateAccess,
  schema,
  updateToolModule,
} from "./update-module.js";
